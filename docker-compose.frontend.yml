services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trackerz-frontend
    restart: unless-stopped
    environment:
      NODE_ENV: "production"
      PORT: "3000"
      NEXT_PUBLIC_BACKEND_URL: "http://103.208.26.38:4000"
      BACKEND_URL: "http://103.208.26.38:4000"
    ports:
      - "3000:3000"
